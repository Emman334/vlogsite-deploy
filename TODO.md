# TODO - Convert PHP blog to React

- [ ] Create React (Vite) frontend in `react-app/`.
- [ ] Implement UI components (Header/Nav, CustomizationPanel, Footer, Pages).
- [ ] Implement API client calls to fetch home/article/podcast data.
- [ ] Implement content creation in React (multipart upload) calling PHP API.
- [ ] Implement like/dislike in React calling PHP API endpoints.
- [ ] Copy existing CSS into React-friendly stylesheet and wire theme/customization behavior.
- [ ] Implement PartsJS integration (or safe fallback if library not present).
- [ ] Start dev server and verify rendering + interactions.
- [x] Document how to run backend PHP API (and required DB schema/columns).
- [ ] Update main/root entry (optional) to serve React build from PHP if needed.


